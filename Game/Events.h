#ifndef EVENTS
#define EVENTS

// Define some strongly typed enumerations which will act as the keys for the event logic
enum class Events
{
    Window_Close,
	W_Pressed,
	W_Released,
	A_Pressed,
	A_Released,
	S_Pressed,
	S_Released,
	D_Pressed,
	D_Released
};

#endif